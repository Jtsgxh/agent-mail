# 第一版验收记录

## 移除 Claude 自主创建（2026-09-11）

- 用户要求撤下未完成验收的 Claude 自主创建功能。删除桌面链接创建器、专用状态处理、CLI 帮助和 README 中的创建流程，以及 Claude 创建 smoke 入口和相关测试。技能中的创建指引已在前一提交移除。
- CLI 创建/查询命令、HTTP 创建记录路由和网页展示收敛到 Codex，删除 Claude 创建分支。已有 Claude 的成员关系、原生接入、Channel、读信、回复和 ACK 保留。
- 全部 78 项自动化测试、语法检查及 diff 检查通过，覆盖 Codex 创建、登记、并发与持久化，以及已有 Claude 的原生通知、Channel 协议和收发信。
- 实际 Edge 浏览器在隔离信箱验证：仅显示 Codex 创建记录，已有 Claude 参与者正常显示，不再请求 Claude 创建记录；页面无脚本错误。
- 本轮未启动模型或操作 Claude 桌面，也未重启运行中的正式信箱；服务代码在下次正常启动时加载。

## App 任务自动登记侧栏（2026-09-10）

- 本机 App 26.903.9818.0 的工具创建路径将启动内容记为 tool output；实际讨论任务有名称和回信，但 SQLite 的 preview 为空。普通 thread/list 的过滤包含 `threads.preview <> ''`，因此不能把创建成功或按 ID 可读等同于普通侧栏可见。
- 使用 App 自身的侧栏接口创建「Agent Mailbox」分组，把正式讨论任务 `01a08a83-238a-7271-8b46-93b27a17d64d` 放入其中。App 返回该分组包含对应任务，用户也明确确认“能看到了”。没有修改 App 数据库、安装文件或向讨论任务发送占位消息。
- 自动流程在接入/创建阶段准备唯一分组，新任务在自己执行 `topic join` 时，以自己的 App 上下文和正式任务 ID 登记侧栏，再登记通知入口。工作区准备编号不会拿来移动任务。已置顶或已在其他分组中的任务保留用户选择；分组缺失、重名或登记失败会明确报错，不重新创建任务。
- 79 项自动化测试、语法及 diff 检查通过。真实 CLI 子进程与模拟 App 验证提前加入、延迟加入、工作区准备、并发分组准备、保留已有位置、侧栏失败不返回加入成功，以及重复创建拒绝。新 helper 对当前真实任务读取并确认了已有侧栏归属；本轮没有再次启动模型或新建实测任务。
- 当前正式服务的原分组已通过 App 接口准备完成，CLI 的自动登记步骤即时生效。后台新增的分组准备逻辑在下次正常启动服务时加载；无需中断正在讨论的任务。仓库及两端已安装的 skill 同步更新。

## 复用当前 Codex App（2026-09-10）

- 默认创建已改为 `Claude CLI → Mailbox → 当前桌面 App 的 create_thread → 新任务自行加入`；后续通知使用 App 的 send_message_to_thread。显式 `--endpoint` 才走旧 WebSocket 后端，不读取旧默认地址或自动启动 4500。
- 对照本机 Codex App 26.903.8094.0 的原生工具管道及所附 codex-app-tools MCP 实现核对帧格式、真实调用任务上下文、独立请求编号、项目选择、创建结果和权限继承。该协议属于内部接口；App 升级后的兼容性不能视为永久保证。
- 75 项自动化测试、语法检查及 diff 检查通过。新增覆盖分片管道通信、可用性检查不改业务数据、Claude CLI 无 App 环境仍可创建、工作区准备编号与正式任务绑定、提前/延迟登记、重复创建拒绝、调用任务隔离、首条指令失败、不登记超时、删除取消及投递结果核对。
- 经用户明确授权，`node scripts/smoke-sessions.js codex E:\UnityProject` 在当前 App 创建临时任务 `01a08a7a-a8af-7690-8891-0423d774152f`。隔离主题 `60e38efe-390d-4d4d-8806-d2d24a3ee8cf` 中，新任务自行登记并回信 #1；定向消息 #2 收到回信 #3 和 ACK。脚本退出码 0，临时信箱已关闭；通过 App 任务工具确认完成后归档。证据保存在本机临时目录 `mailbox-live-session-r3Pli4/proof.json`。没有在正式主题中代替 Claude 发起新讨论。
- 首次更新正式服务的进程重启命令被自动审批拒绝，未执行。之后观察到正式 Mailbox 已由新进程加载；按用户命令执行 `mailbox codex app connect` 成功，正式 `/api/codex/status` 返回 `reachable / desktop-app`，网页显示“Codex App · 已连接”并正确展示接入说明。
- 正式数据库已备份到忽略提交的 `.mailbox/backups/before-desktop-reuse-20260910.db`；新服务加载后核对七张业务表，原有字段和记录内容哈希全部一致。新 App 接入信息仅存在服务内存，未写入数据库。仓库及 Codex、Claude 两处安装的 skill 内容一致且校验通过。

## Codex 创建服务启动与连接（2026-09-10）

- 本次用户实测失败的原因是 4500 无监听，同时旧 Mailbox 后端对状态接口返回 404。网页现在提供“Codex 创建服务 → 启动并连接”，复用启动脚本的实际启动、握手与配置保存逻辑；不是仅恢复检测指示器。
- 62 项测试、语法检查和 diff 检查通过。新测试覆盖首次启动与配置保存、重复点击合并、已有服务复用、占用端口不替换、子进程退出/超时清理、服务关闭取消启动、空/无效配置不回退及凭据不泄露。三个 skill 副本校验通过且内容一致。
- 正式信箱已加载启动接口，浏览器从“未就绪”实际点击启动，变为“可用”，`ws://127.0.0.1:4500/` 由真实 Codex 进程监听。共享服务继续运行，未改变桌面 Codex 的私有后端。
- `node scripts/smoke-sessions.js codex` 真实通过：临时主题 `12eab743-4668-4d01-b7da-e16fb888a97c` 中新 Codex `01a08a38-0750-7921-bdc1-8c4b6c174ebe` 主动登记、首次回复 #1，第二封定向信 #2 收到回复 #3 并 ACK。临时会话已归档，测试信箱已关闭。此项验证创建入口及新 Codex 行为，没有代替用户的 Claude 在正式主题里发起讨论。
- 正式数据库已用 SQLite backup 备份至忽略提交的 `.mailbox/backups/before-codex-service-20260910.db`，更新前后七张业务表逐表内容哈希一致。重启清除了原 Claude 的内存通知入口，需原会话用原身份重新加入；未复制或持久化其凭据。
- 最后补充的空/缺失 endpoint 配置校验已通过测试，但第二次后端重启被自动审批拒绝，未提供具体原因。现有有效配置下的启动和真实收发已验证；该校验修正需下次正常重启加载。

## Codex 宿主与会话通知状态（2026-09-10）

- `npm run check`、`git diff --check` 与 55 项测试通过。新测试覆盖配置读取与环境变量优先级、未配置、无效地址、凭据不泄露、WebSocket 与初始化超时、认证拒绝、无效协议，以及正常握手后的连接释放。
- HTTP 状态检测前后，主题、参与者、会话绑定和公共状态保持不变；模拟宿主仅收到 `initialize` 与 `initialized`，未创建或恢复 thread，未启动 turn。关闭模拟宿主后再次查询返回连接失败，不复用旧的可连接结果。
- 独立内存信箱与模拟宿主上的浏览器验证顶部“Codex 宿主 · 可连接”、展开后的地址和检测时间、手动重新检测，以及参与者和独立会话的已登记/未登记状态。浏览器未报告错误或警告。Claude 不增加宿主服务检测，仍展示自身通知入口登记状态。
- 正式信箱的 `/api/codex/status` 探测仍返回旧版“接口或资源不存在”，本次未重启正式服务；需由用户重启正在使用的 Mailbox 服务加载接口。没有启动真实 Codex / Claude 宿主或调用模型，临时验收进程在结束后关闭。

## 项目管理与删除错误提示（2026-09-10）

- `npm run check`、`git diff --check` 和 47 项自动化测试通过。项目改名保留 ID，校验空名称、长度和重名；删除项目将主题归属设为 NULL 后删除项目，事务失败会回滚。重启后消息、回执、成员进度、参与者、会话绑定和无关项目归属保持不变。
- 实际 HTTP 与 CLI 验证按名称或 ID 改名、删除空项目及含主题项目、不存在项目返回 404，以及 SSE 观察者取得更新后的项目名称与主题归属。
- 独立内存服务上的浏览器验证项目操作入口、改名成功、重名错误直接显示在弹窗内、删除确认及取消。CLI 删除项目后，页面切到“未归类”，当前讨论、消息和未发送草稿保留。未调用真实 agent 模型。
- 本机旧进程曾返回“接口或资源不存在”，随后主题删除接口已加载。本次已备份正式数据库并重启 Mailbox，项目 PATCH/DELETE 与主题 DELETE 的不存在 ID 探测均返回对应业务错误，证明路由已加载；重启前后七张表内容哈希完全一致。浏览器在正式服务选择 roguetower 后已显示“改名”和“删除项目”。

## 删除主题（2026-09-10）

- `npm run check`、`git diff --check` 和 44 项自动化测试通过。新增测试覆盖主题及回复引用、消息回执、成员进度和会话绑定的事务删除，失败回滚、重启持久化、外键完整性，以及共享参与者和其他主题保持不变。
- 实际 CLI 经 HTTP 删除临时主题，验证 SSE 通知、删除后查询和发言返回 404。服务直接通知在传输期间遇到删除会取消本主题传输，随后继续其他主题；旧版原生监听器、Claude Channel、Codex Bridge 在回执写入前遇到删除也保持连接。宿主使用模拟程序或协议端，未调用真实模型。
- 独立内存服务上的浏览器检查了删除按钮、确认框和取消操作；取消后消息保留，CLI 删除后页面自动退出已删除的主题并显示空列表。测试数据未写入正式信箱。
- 正式信箱的 4 个主题、32 条消息等七张表已保存升级前的数量和内容哈希快照；停止并重启正式服务的操作被自动审批拒绝，未升级正在运行的服务。需重启 Mailbox 后使用新删除接口；已创建或正在启动的原生 agent 会话不由本功能结束。

## 按 topic 创建独立会话（2026-09-09）

- `npm run check` 和 37 项自动化测试通过。新测试覆盖真实 CLI/HTTP/SQLite、模拟 Claude/Codex 宿主、真实本机管道、并发只创建一次、跨重启绑定保留、离线/暂停前置拒绝、启动结果不明和已提交但未登记超时。模拟宿主验证不能替代真实模型验收。
- 本机 Codex CLI 0.153.4 已启动独立常驻 App Server：`ws://127.0.0.1:4500`。握手成功，连接信息保存在忽略提交的 `.mailbox/codex-host.json`。没有重启或改变桌面 Codex 的 stdio 后端。
- `node scripts/smoke-sessions.js codex` 真实通过：独立会话 `01a08582-7fdf-73b1-98c3-34b297d7c602` 在新主题中自行登记、回复 #1；第二次来信 #2 经 queue 投递后回复 #3 并 ACK。主题 ID 为 `44facb37-fa4d-4f97-b4a9-7ece27a3e3b1`。测试会话已归档，临时信箱服务已停止，共享 App Server 保持运行。
- 首次真实尝试暴露出只读环境不能创建 `--body-file` 的问题。启动提示已改用 `--body` / `--stdin`，不增加文件写权限；受阻测试轮次已中断并归档。
- 本机 Claude 2.1.238 的 `agents --json` 在验收时已没有活跃进程；当时的 Claude 创建验收按约定在预留身份之前失败（该验收入口现已移除）。真实 `--bg` 新会话、原生 ID 与新会话实际 ID 一致性、登记及两轮回信尚未验证；没有启动离线 Claude 来绕过此条件。
- CLI 成功返回仍只证明收件入口登记；原生队列提交、实际回信和 ACK 是不同证据。Claude 进程退出/重启后的管道恢复，以及机器重启后的宿主自启动不在本次实现范围内。
- 本机 Mailbox 服务已重启加载新接口；升级前后 3 个主题、20 条消息及参与者记录的 SHA-256 校验一致。原有通知入口按设计清除，需要各原会话重新加入。仓库技能与 Codex、Claude 安装副本哈希一致，skill-creator 校验通过；重复启动宿主脚本通过握手复用已有服务。

日期：2026-09-09。环境：Windows、本机 Node.js 24.18.0、Codex CLI 0.153.2、Claude Code 2.1.238。

## 自动化

`npm test` 验证持久化、阅读进度、定向通知、分页、幂等发送、暂停/恢复、断线后未确认消息重投、同一参与者重复连接拒绝、跨站请求拒绝、CLI 进程、Claude MCP 协议、Codex 状态切换及输出错误处理。还通过实际启动 CLI 子进程，验证了 Claude MCP stdio 初始化、工具发现与通知收取。

Codex 协议测试使用模拟 App Server；Claude 协议测试使用官方 MCP SDK，并不意味着真实 Claude 模型已经消费 Channel 事件。

## 真实 Codex

已运行 `scripts/smoke-codex.js`。独立 App Server 中创建的测试会话完成初始轮次并进入空闲，然后：

1. 发第一封定向消息，真实模型回复“第 1 次收信成功”，回信写入信箱，原消息确认。
2. 同一会话再次处于空闲，发第二封信，真实模型回复“第 2 次收信成功”，回信与确认成功。
3. 停止桥接、归档测试会话、关闭测试 App Server。

额外发现：临时、没有持久化记录的会话不能经 `thread/resume` 恢复。正式桥接要求已有可恢复的会话，不用猜测或复制会话状态绕过这个限制。

## 网页

使用 Codex 内置浏览器实际操作本机服务，检查空状态、创建主题、Markdown/代码块消息、创建参与者及连接说明、定向通知显示待投递、暂停/恢复、关闭后禁用发送、重新打开，以及人工确认已读。浏览器未报告 JavaScript 错误或警告。

## 简化连接入口（旧版，已被原生通知接入替换）

新增 `mailbox connect` 后，19 项自动化测试通过，包括参与者名称解析、只读预览、Windows npm 启动脚本解析、实际 CLI 子进程列会话、选择已有会话后完成回复与确认，以及初始化失败时释放所启动的进程。

本机真实 Codex 验证：先结束独立测试会话的原进程，再通过 `connect` 自动启动 stdio App Server 并恢复同一会话，收到真实模型回复“简化接入成功”，回信写入并确认。测试会话已归档。运行中原有 Codex 配置的一个本地 MCP 服务报告连接失败，此项未修改；本次信箱回信仍完成。

网页实际检查了 Claude 的简化命令展示。Claude 新入口的参数生成和 MCP stdio 通道经过测试，但用户终端中的会话选择与自定义 Channel 确认仍需实际操作，不能视为已完成真实 Claude 自动唤醒验收。

## 原生通知接入（旧版独立通知进程）

`npm run check` 与 20 项自动化测试通过。新增测试覆盖 Codex queue 参数及失败保留未确认、暂停/恢复、重连重投、Windows 实际命名管道的认证及消息帧、后台通知进程订阅成功才返回，以及 disconnect 让通知进程退出。原生通知不创建或恢复 agent，不代发回信、不自动 ACK。

已运行 `node scripts/smoke-codex.js CODEX_ENTRY --native`（CODEX_ENTRY 为本机 CLI 实际入口）：在独立测试 App Server 中，同一个真实 Codex 会话两次从空闲收到 `codex queue` 通知并产生模型响应。测试指令禁止调用工具，模型明确表示未读信、未回信、未确认；两次 mailboxAutoAck 均为 false。测试会话已归档，测试服务已关闭。此项证明原生队列可以触发同一会话的新轮次，不意味着模型已经完成信箱收发闭环。

Claude 原生入口的测试使用真实本机命名管道和模拟接收端，验证了 auth/user NDJSON 帧及不可达错误；没有向用户其他 Claude 会话投递测试消息，尚未验证真实 Claude 模型自动消费。管道写入成功仅表示传输完成，不能证明接收策略放行或模型处理成功。

本机信箱服务已重启加载新接口，原讨论保留。内置浏览器实际检查 Codex / Claude 连接按钮，均显示目标会话内部执行的 --background 命令及原生入口说明；浏览器未报告 JavaScript 错误或警告。两端已安装的 skill 与仓库版本 SHA-256 一致，skill 校验通过。

## 真实会话往返（旧版通知进程）

用户已在实际会话完成双向验收：Codex 定向 #5 后 Claude 自动收到提示并回 #6、确认；Claude 定向 #7 后当前 Codex 自动收到提示并回 #8、确认。#6 不带收件人，是通过 wait 查收，不能把 #6 当成反向唤醒证明；反向证据是 #7。

## 加入即登记、服务直接投递（当前版本）

24 项自动化测试及语法检查通过。实际 CLI 子进程用自身环境中的会话 ID 加入，服务随后直接调用 queue，state.bridges 为空。新增测试还覆盖同一入口重复加入不重投、拒绝改绑、暂停保留待投递消息、失败后显式重新加入才重试、真实本机 Claude 管道认证帧、凭据不出现在公共响应，以及服务重启保留消息但清除入口。

已重新运行真实 Codex smoke 的 --native 模式：加入请求携带入口后，服务直接投递两次通知，独立测试 App Server 的同一会话两次从空闲产生模型响应，期间没有 bridge 订阅。模型仍被测试指令禁止调用工具，两次 mailboxAutoAck=false。测试会话和服务已清理。

本机服务已更新，当前 codex-review 用 topic join 完成直接入口登记。浏览器显示“通知入口已登记”和“参与方式”，Claude 的提示只需运行 topic join，无需 connect。原 Claude 入口属于旧通知进程，升级重启后需在其自身会话重新加入，不能沿用旧进程测试来宣称新版 Claude 模型收信已验收。两端安装的使用 skill 已同步并通过校验。

## 项目归类（当前版本新增）

26 项自动化测试及语法检查通过。新增测试从旧版 topics 表迁移到可选 project_id，验证历史消息、已读进度及重启持久化；另经实际 HTTP 与 CLI 子进程验证项目创建、按名称/ID 筛选、未归类筛选、主题移动、无效项目拒绝，以及移动不改变待投递消息。

独立内存测试服务上，浏览器实际完成创建项目、项目内新建讨论、筛选未归类主题、将旧主题移动到项目，确认数量和归属同步更新、旧消息保留且控制台无错误。临时测试数据未写入正式信箱。

正式服务启动前已备份本机数据库文件，启动后两条历史主题仍在未归类下，分别保留 4 条和 10 条消息。页面已加载项目筛选与所属项目选择器；codex-review 已重新登记通知入口。两端已安装的 skill 已同步并通过校验。

## 批量通知与广播

30 项自动化测试及语法检查通过。新增测试覆盖多个收件人去重、消息只存一次、投递错误与确认按收件人隔离、无效收件人导致整次发送失败，以及广播按发送时的成员名单保存、排除发送者、重试不追加后来加入者。旧版单收件人数据库迁移测试保留历史消息、投递状态和确认记录，并验证重启后的批量发送与外键完整性。

实际 CLI 子进程经 HTTP 验证重复 `--to` 和 `--broadcast`；两个模拟原生通知入口分别成功和失败，确认互不阻塞。此项没有向真实 agent 会话发送广播，不代表真实模型已完成批量收发。

独立内存服务上的浏览器操作验证多选发送、广播发送、逐人状态显示，以及切换主题后保留草稿和通知选择；控制台没有错误或警告。正式服务已备份数据库并升级，原 topic2 的消息及确认状态保留，页面已加载新选择器，当前 Codex 会话已重新登记通知入口。两端使用 skill 已同步并通过校验。

## Agent 主动获取讨论目标

37 项自动化测试及语法检查通过。原生 Claude/Codex 通知和旧版 Codex Bridge 均只提供主题 ID 与 `topic show` 指引，并验证通知文本不包含目标原文。Claude Channel 新增 `mailbox_topic` 工具，协议测试验证由宿主主动调用后取得 `topics.goal`。此项没有执行真实 Claude/Codex 模型收信，不代表模型在所有宿主中都会遵循提示发起调用。

## 尚未证明的部分

- 新版由信箱服务直接投递到真实 Claude 模型的完整收发。本机管道与认证协议测试已通过，实际会话需重新加入后验证；旧版自定义 Channel 的真实模型消费也未验收。
- 任意 Codex 桌面或 CLI 会话都能被 queue 路由到。已验证独立测试 App Server，以及旧版投递到当前桌面会话；实际路由仍取决于目标宿主，必要时明确提供 --endpoint。
- 多机、多用户认证隔离、自动启动 agent、自动恢复已退出的 agent 进程，不属于本版本功能。

## Codex 路由 cookie 与无感恢复（2026-09-16）

- Codex 桌面任务首次 `topic join` 后，CLI 在本机 cookie jar 保存随机路由 cookie；SQLite 新增的 `notification_routes` 只保存 SHA-256 哈希、参与者、任务 ID 和消息上限，不保存原始 cookie 或 App 管道。普通路由 CLI/state 输出不包含 cookie、任务 ID 或管道。
- Mailbox 重启后持久路由显示为等待 App。任一 Codex 任务执行普通 `mailbox` 命令时，CLI 自动提交同一信箱的 cookie jar 与当前 App 管道；服务验证哈希后一次恢复全部匹配路由，未确认且尚未成功投递的消息自动续投。显式 `mailbox codex app connect` 使用同一批量恢复路径。
- 路由 cookie 只能恢复原参与者绑定的原任务，不能更换任务；`disconnect` 同时注销内存入口、删除服务端哈希并让 CLI 删除本地 cookie。Claude 原生入口/token 仍只在内存中，不进入 Codex cookie jar。
- 新增真实 SQLite 重启、App 管道换址和 CLI 子进程测试：首次加入生成 cookie，确认原文不出现在 CLI/state/SQLite；重启后不执行 `topic join`，仅运行普通 `mailbox inbox` 即恢复并把待投递消息送到原任务；随后重复 `codex app connect` 不重复发送。另覆盖并发 cookie jar 更新、注销撤销、未知结果阻止自动重放。`npm test` 97/97，`npm run check` 和 `git diff --check` 通过。
